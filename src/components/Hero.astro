<section class="min-h-screen flex flex-col justify-center items-center text-center bg-gradient-to-b from-white via-slate-50 to-slate-100 px-6 py-32 overflow-hidden relative font-sans">
  <div class="z-10 relative space-y-6">
    <p class="text-green-900 font-semibold text-xs md:text-sm tracking-widest uppercase">BIENVENIDO A</p>
    <h1 class="text-5xl md:text-7xl font-extrabold text-darkBlue leading-tight tracking-tight">
      FUHR MARKETING
    </h1>
    <p class="text-darkBlue/60 text-base md:text-lg max-w-xl mx-auto leading-relaxed">
      Una agencia que combina estrategia, diseño y tecnología para hacer crecer marcas locales.
    </p>
  </div>

  <img
    id="iso-spin"
    src="/images/hero.jpg"
    alt="Isotipo FUHR"
    class="absolute w-72 md:w-96 top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 z-0 opacity-100 pointer-events-none"
  />

  <script>
    const img = document.getElementById('iso-spin');
    const maxRot = 360;
    window.addEventListener('scroll', () => {
      const progress = window.scrollY / (document.body.scrollHeight - window.innerHeight);
      img.style.transform = `translate(-50%, -50%) rotate(${progress * maxRot}deg)`;
    });
  </script>
</section>
